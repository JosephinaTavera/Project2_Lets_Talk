<html>
<head>
	<title></title>
</head>
<body>

	<h1> <%= list.title %> </h1>

	<form action= 'topic/comment/new'method=POST>
		<input type='submit' value='New Comment' />
	</form>

	<form action='topic/<%=list._id%>/comment/search' method=POST>
		<input type = 'text' name='comment' placeholder='search'>
	</form>

	<% if (list.comment == undefined) {%>
		<p>No comments to display</p>
	<%} else { %>
		<% for (var i=0; i < list.length; i++ ){ %>
			<li> <%= list.comment[i] %> </li>
		<%} %>
	<%} %>

</body>
</html>